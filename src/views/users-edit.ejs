<%- include('partials/_header')%>

    <div class="container mt-5">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="text-center mt-2">Actualizar Usuario</h3>
                </div>
                <div class="card-body">
                    <form action="/users/update/<%= data[0].idUsuario %>" method="POST">
                        <div class="form-group">
                            <input type="text" name="nombreCompleto" placeholder="Nuevo Nombre" value="<%= data[0].nombreCompleto %>" class="form-control">
                        </div>
                        <div class="form-group">
                            <input type="text" name="username" placeholder="Nuevo Nombre de Usuario" value="<%= data[0].username %>" class="form-control">
                        </div>
                        <div class="form-group">
                            <input type="password" name="password" placeholder="Nueva Contraseña" value="<%= data[0].password %>" class="form-control">
                        </div>
                        <div class="form-group">
                            <input type="text" name="tipo" placeholder="Nuevo Tipo de Usuario" value="<%= data[0].tipo %>" class="form-control">
                        </div>
                        <button type="submit" class="btn btn-primary">Actualizar Proveedor</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <%- include('partials/_footer')%>